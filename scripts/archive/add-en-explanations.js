'use strict';
// One-off migration: turn each question's `explanation` string into
// { en, ar }, preserving the existing Arabic text and code snippets.
const fs = require('fs');
const path = require('path');
const file = path.join(__dirname, '..', 'src', 'data', 'questions.json');

const en = {
  1: "The print() function written this way, without a semicolon, is distinctive to Python.",
  2: "The def keyword and an f-string are clear Python markers.",
  3: "The __name__ == \"__main__\" pattern is specific to Python.",
  4: "A list comprehension with the ** power operator is a Python feature.",
  5: "The with statement (context manager) is characteristic of Python.",
  6: "import ... as syntax and the numpy library are very common in Python.",
  7: "The @ decorator above a def points to Python (Flask here).",
  8: "async def and async with are Python's asynchronous syntax.",
  9: "A dict comprehension with 'is not None' is Python.",
  10: "lambda with map/filter like this strongly indicates Python.",
  11: "console.log is the standard way to print in JavaScript.",
  12: "const with the arrow function => is modern JavaScript.",
  13: "document.querySelector and innerHTML are JavaScript DOM APIs.",
  14: "setTimeout with an arrow function is a JavaScript API.",
  15: "Object destructuring is common in JavaScript/ES6.",
  16: "fetch() and res.json() are browser APIs specific to JavaScript.",
  17: "A Promise written this way is core to JavaScript's async model.",
  18: "export default is ES module syntax in JavaScript.",
  19: "Chaining filter().reduce() with arrow functions is a JavaScript pattern.",
  20: "The spread operator ... inside an object is a JavaScript ES feature.",
  21: "#include <iostream> and std::cout are sure signs of C++.",
  22: "std::cout << and std::endl are core C++.",
  23: "std::vector with the std namespace is specific to C++.",
  24: "template <typename T> is C++ generic templates.",
  25: "new/delete and manual pointer management are C++.",
  26: "virtual ... = 0 (a pure virtual function) is C++.",
  27: "std::sort with begin()/end() is from the C++ standard library.",
  28: "std::make_unique and smart pointers are modern C++ (C++11+).",
  29: "const auto& with std::cout is a modern C++ pattern.",
  30: "std::map<std::string, int> with the std namespace is C++.",
  31: "The public static void main(String[] args) signature is Java's fingerprint.",
  32: "System.out.println is Java's standard print method.",
  33: "A capitalized public class matching the file name is a Java pattern.",
  34: "ArrayList<String> ... new ArrayList<>() is from Java collections.",
  35: "@Override with a public String signature is Java.",
  36: "A for-each loop with System.out.println indicates Java.",
  37: "The access modifiers private final before the type are Java traits.",
  38: "stream() with Collectors.toList() is Java's Stream API.",
  39: "A 'throws IOException' clause in the signature is specific to Java.",
  40: "HashMap<>() with map.put is from Java collections.",
  41: "The println! macro with an exclamation mark and the fn keyword are sure signs of Rust.",
  42: "let mut for mutable variables is from Rust.",
  43: "The i32 types and the -> return arrow are Rust syntax.",
  44: "The vec! macro and the Vec<i32> type are specific to Rust.",
  45: "match with Some/None is a Rust pattern (Option).",
  46: "A struct with name: type fields and f64 types is Rust.",
  47: "The 0..10 range with println! indicates Rust.",
  48: "The ? error-propagation operator and std::fs are Rust.",
  49: "impl Trait for Type and &self are from Rust's trait system.",
  50: "String::from and the &str (slice) type are from Rust's ownership model.",
  51: "package main, fmt.Println and the func keyword are sure signs of Go.",
  52: "The type comes after the parameter name (a int) — a Go pattern.",
  53: "The 'if err != nil' error-handling pattern is a Go fingerprint.",
  54: "The short assignment operator := is specific to Go.",
  55: "The defer keyword for deferring execution is from Go.",
  56: "type Name struct with fields typed after the name is a Go pattern.",
  57: "map[string]int with := assignment is Go syntax.",
  58: "make(chan int) and the go keyword to launch a goroutine are Go.",
  59: "A receiver (func (p *Point)) before the function name is a Go pattern.",
  60: "fmt.Printf with the %v verb is from Go's fmt package.",
  61: "A for ... in range() loop with print and no braces is a Python pattern.",
  62: "The input() function for reading input is distinctive to Python.",
  63: "The list.append() method and list building are from Python.",
  64: "try/except with ValueError and a colon after the statement are Python.",
  65: "The __init__ constructor and the self parameter are from Python classes.",
  66: "The str.join() method on a separator string is a Python pattern.",
  67: "from collections import Counter is from Python's standard library.",
  68: "yield from for generator delegation is specific to Python.",
  69: "sorted(key=lambda, reverse=True) is Python syntax.",
  70: "pandas DataFrame and column access are very common in Python.",
  71: "let inside a for loop is modern JavaScript.",
  72: "prompt() is a browser API specific to JavaScript.",
  73: "forEach with an arrow function and console.log are JavaScript.",
  74: "addEventListener is a JavaScript DOM API.",
  75: "JSON.stringify with the {} object is a JavaScript pattern.",
  76: "class extends with constructor and super() is JavaScript class syntax.",
  77: "Set with the spread operator ... to dedupe is from JavaScript.",
  78: "req/res and res.json are from Express in JavaScript/Node.",
  79: "useState and array destructuring are from React (JavaScript).",
  80: "Promise.all with map and fetch are from async JavaScript.",
  81: "using namespace std and #include with angle brackets are C++.",
  82: "std::cin >> for reading input is from C++.",
  83: "std::string with std::cout and the std namespace are C++.",
  84: "An explicit begin()/end() iterator with auto and ++it is C++.",
  85: "A struct with a self pointer Node* and a semicolon after } is C++.",
  86: "The cout/cin combination with the std namespace is C++.",
  87: "std::make_shared and the shared_ptr smart pointer are modern C++.",
  88: "std::setprecision and std::fixed are from C++ iomanip.",
  89: "vector<pair<>> with the std namespace is from C++ containers.",
  90: "template <class T> and a class template are from C++.",
  91: "The [] array syntax new int[10] with the type before the name is Java.",
  92: "String with System.out.println is from Java.",
  93: "Scanner(System.in) for reading input is specific to Java.",
  94: "abstract class and an abstract method are from Java.",
  95: "e.printStackTrace() inside a catch is from Java.",
  96: "Nested generic types and new HashMap<>() are from Java collections.",
  97: "The :: method reference (System.out::println) is from Java 8+.",
  98: "Optional.ofNullable is from Java.",
  99: "The @SpringBootApplication annotation is from the Spring framework in Java.",
  100: "StringBuilder with append() is from Java.",
  101: "let with the i32 type is from Rust.",
  102: "The println! macro with {} placeholders is from Rust.",
  103: "iter().sum() and the i32 type are from Rust.",
  104: "An enum written this way is from Rust.",
  105: "String::new() and the usize type are from Rust.",
  106: "The if let Some(...) pattern is from Rust.",
  107: "Trait bounds <T: PartialOrd> and the slice &[T] are from Rust.",
  108: "A closure with |x| and a -> return type is from Rust.",
  109: "use std::collections and let mut are from Rust.",
  110: "The Result<i32, String> type and Ok are from Rust.",
  111: "var name string (type after the name) is from Go.",
  112: "The []int slice with short assignment := is from Go.",
  113: "fmt.Sprintf with := is from Go.",
  114: "for ... range with index, value and := is from Go.",
  115: "An interface with simple method signatures is a Go pattern.",
  116: "WaitGroup with go func and defer are from Go concurrency.",
  117: "Multiple return result, err and strconv.Atoi are from Go.",
  118: "select with a channel <-ch is from Go.",
  119: "The built-in append on slices is from Go.",
  120: "A receiver (s *Server) and the http.ResponseWriter signature are from Go."
};

const data = JSON.parse(fs.readFileSync(file, 'utf-8'));
let missing = 0;
for (const q of data) {
  const ar = typeof q.explanation === 'string' ? q.explanation : (q.explanation && q.explanation.ar) || '';
  if (!en[q.id]) { missing++; console.error('Missing EN for id', q.id); }
  q.explanation = { en: en[q.id] || ar, ar };
}
fs.writeFileSync(file, JSON.stringify(data, null, 2) + '\n', 'utf-8');
console.log(`Updated ${data.length} questions; missing EN: ${missing}`);
