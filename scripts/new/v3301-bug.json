[
{ "id": 196, "category": "javascript", "difficulty": "easy", "codeSnippet": "const user = { name: 'Ana' };\nconsole.log(user.Name);", "question": { "en": "Which fix removes the bug?" }, "options": ["Match the key's casing", "Quote the property name", "Use bracket notation", "Freeze the object first"], "answer": "Match the key's casing", "explanation": { "en": "Property names are case-sensitive, so `Name` is a different key and reads as undefined." } },
{ "id": 197, "category": "python", "difficulty": "easy", "codeSnippet": "age = input('Age: ')\nprint(age + 1)", "question": { "en": "Which fix removes the bug?" }, "options": ["Convert with int() first", "Use print(age, 1) instead", "Declare age as global", "Call str() on the 1"], "answer": "Convert with int() first", "explanation": { "en": "input() always returns a string, and adding an int to a str raises TypeError." } },
{ "id": 198, "category": "javascript", "difficulty": "medium", "codeSnippet": "const copy = JSON.parse(JSON.stringify(event));\nconsole.log(copy.date.getTime());", "question": { "en": "Which fix removes the bug?" }, "options": ["Revive the date explicitly", "Use Object.assign instead", "Spread the object first", "Call toJSON() before"], "answer": "Revive the date explicitly", "explanation": { "en": "JSON has no date type, so the round trip turns the Date into a string with no getTime method." } },
{ "id": 199, "category": "python", "difficulty": "medium", "codeSnippet": "import datetime\ndef log(msg, at=datetime.datetime.now()):\n    print(at, msg)", "question": { "en": "Which fix removes the bug?" }, "options": ["Default at to None", "Import now directly", "Use a lambda default", "Make at keyword-only"], "answer": "Default at to None", "explanation": { "en": "Defaults are evaluated once at definition, so every call prints the same timestamp." } },
{ "id": 200, "category": "typescript", "difficulty": "hard", "codeSnippet": "type Shape = { kind: 'circle'; r: number } | { kind: 'square'; s: number };\nfunction area(x: Shape): number {\n  if (x.kind === 'circle') return Math.PI * x.r ** 2;\n}", "question": { "en": "Which fix removes the bug?" }, "options": ["Handle the square branch", "Widen kind to a string", "Mark r as optional", "Return any from area"], "answer": "Handle the square branch", "explanation": { "en": "Squares fall through and return undefined; an exhaustive switch with a `never` check catches this." } }
]
