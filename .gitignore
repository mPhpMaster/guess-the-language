# Dependencies
node_modules/

# Build output
dist/
dist-web/
release/
out/

# Logs
*.log
.playwright-cli/
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local credentials for the archived v1 app (see its *.example.js templates)
v1/src/supabase-config.js
v1/src/discord-config.js

# Environment variables (Vite / Vercel build)
.env
.env.*
!.env.example

# Vercel
.vercel

# Electron Builder
electron-builder.env

# OS / editor
.DS_Store
Thumbs.db
Desktop.ini
.vscode/
.idea/
*.swp
*~

# Claude Code local session data
.claude/

.vercel
.env*
# This blanket rule shadows the earlier "!.env.example", so re-allow the
# templates — they carry no values and are the only record of which keys exist.
!.env.example
!**/.env.example

# Runtime config with local credentials (see *.example.js)
public/supabase-config.js
public/discord-config.js
