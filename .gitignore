# Dependencies
node_modules/

# Build output
dist/
dist-web/
release/
out/

# Logs
*.log
.playwright-cli/
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local Supabase credentials (use supabase-config.example.js as a template)
src/supabase-config.js
# Same file, generated into publicDir for the Vite build/dev server.
public/supabase-config.js

# Local Discord client ID (use discord-config.example.js as a template)
src/discord-config.js
public/discord-config.js

# Local admin passcode used by the session tests. Holds the real ADMIN_PASSCODE,
# and this is a public repo — it was untracked but not ignored, which is one
# `git add -A` away from being published permanently in the history.
test/session-passcode.js

# Environment variables (Vite / Vercel build)
.env
.env.*
!.env.example

# Vercel
.vercel

# Electron Builder
electron-builder.env

# OS / editor
.DS_Store
Thumbs.db
Desktop.ini
.vscode/
.idea/
*.swp
*~

# Claude Code local session data
.claude/

.vercel
.env*
