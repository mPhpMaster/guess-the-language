[
  {
    "id": 1,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "function add(a, b) {\n  a + b;\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `+` to `*`",
      "Rename the function",
      "Add `return` before `a + b`",
      "Remove the parameters"
    ],
    "answer": "Add `return` before `a + b`",
    "explanation": {
      "en": "The function computes the sum but never returns it."
    }
  },
  {
    "id": 2,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "def greet(name):\nprint(\"Hi \" + name)",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Indent the `print` line",
      "Remove the parameter",
      "Use single quotes",
      "Add a colon after print"
    ],
    "answer": "Indent the `print` line",
    "explanation": {
      "en": "The body of the function must be indented under the def."
    }
  },
  {
    "id": 3,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const nums = [1, 2, 3];\nconsole.log(nums.length());",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Use `nums.size` without ()",
      "Use `nums.count` as a method",
      "Use `nums.length` without ()",
      "Use `length(nums)` as a call"
    ],
    "answer": "Use `nums.length` without ()",
    "explanation": {
      "en": "`length` is a property, not a method, so drop the parentheses."
    }
  },
  {
    "id": 4,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "nums = [10, 20, 30]\nprint(nums[3])",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `nums[3]` to `nums(3)`",
      "Use `nums.get(3)`",
      "Add a fourth element silently",
      "Change `nums[3]` to `nums[2]`"
    ],
    "answer": "Change `nums[3]` to `nums[2]`",
    "explanation": {
      "en": "Valid indices are 0..2; index 3 is out of range."
    }
  },
  {
    "id": 5,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "let x = 5\nif (x = 10) {\n  console.log(\"ten\");\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `x = 10` to `x => 10`",
      "Remove the if statement",
      "Change `x = 10` to `x == 10`",
      "Use `let` inside the if"
    ],
    "answer": "Change `x = 10` to `x == 10`",
    "explanation": {
      "en": "A single `=` assigns; use `==` (or `===`) to compare."
    }
  },
  {
    "id": 6,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "total = 0\nfor i in range(5)\n  total += i",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Remove `range` from the header",
      "Add a colon after `range(5)`",
      "Change `+=` to `=+` in the body",
      "Use `while` instead of `for`"
    ],
    "answer": "Add a colon after `range(5)`",
    "explanation": {
      "en": "A `for` statement header must end with a colon."
    }
  },
  {
    "id": 7,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const name = \"Ada\";\nconsole.log(\"Hello, \" + Name);",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Add `let Name`",
      "Use backticks",
      "Capitalize the string",
      "Change `Name` to `name`"
    ],
    "answer": "Change `Name` to `name`",
    "explanation": {
      "en": "JavaScript is case-sensitive; the variable is `name`."
    }
  },
  {
    "id": 8,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "def square(n):\n    return n ** 2\n\nprint(squere(4))",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `squere(4)` to `square(4)`",
      "Change `**` to `*` in the body",
      "Add a second argument to the call",
      "Use `pow(4)` from the math module"
    ],
    "answer": "Change `squere(4)` to `square(4)`",
    "explanation": {
      "en": "The call misspells the function name `square`."
    }
  },
  {
    "id": 9,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const arr = [1, 2, 3];\narr.push;",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Use `arr.add(4)` instead of push",
      "Change `arr.push;` to `arr.push(4);`",
      "Use `arr += 4` to append the value",
      "Remove the semicolon after push"
    ],
    "answer": "Change `arr.push;` to `arr.push(4);`",
    "explanation": {
      "en": "`push` must be called with parentheses and a value."
    }
  },
  {
    "id": 10,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "age = \"20\"\nnext_year = age + 1",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `+ 1` to `+ \"1\"`",
      "Use `age++`",
      "Wrap age with `int(age)`",
      "Remove the quotes around 20 only"
    ],
    "answer": "Wrap age with `int(age)`",
    "explanation": {
      "en": "A string cannot be added to an int; convert with `int()`."
    }
  },
  {
    "id": 11,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "function isEven(n) {\n  return n % 2 = 0;\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `% 2` to `/ 2`",
      "Add `let` before n",
      "Change `= 0` to `=== 0`",
      "Return `n % 2`"
    ],
    "answer": "Change `= 0` to `=== 0`",
    "explanation": {
      "en": "Comparison needs `===`, not the assignment operator `=`."
    }
  },
  {
    "id": 12,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "fruits = [\"apple\", \"pear\"]\nprint(len(fruits)",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Use `fruits.length`",
      "Remove `print`",
      "Add a comma after pear",
      "Add a closing `)`"
    ],
    "answer": "Add a closing `)`",
    "explanation": {
      "en": "The `print` call is missing its closing parenthesis."
    }
  },
  {
    "id": 13,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "let count = 0;\nwhile (count < 3) {\n  console.log(count);\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Add `count++` inside the loop",
      "Change `<` to `<=` in the test",
      "Start count at 1 instead of 0",
      "Use a for loop header instead"
    ],
    "answer": "Add `count++` inside the loop",
    "explanation": {
      "en": "Without incrementing count, the loop never ends."
    }
  },
  {
    "id": 14,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const arr = [1, 2, 3];\nfor (let i = 0; i <= arr.length; i++) {\n  console.log(arr[i]);\n}",
    "question": {
      "en": "Which fix removes the off-by-one bug?"
    },
    "options": [
      "Change `i = 0` to `i = 1` in the header",
      "Change `i <= arr.length` to `i < arr.length`",
      "Change `i++` to `i--` in the header",
      "Change `arr[i]` to `arr[i + 1]` in body"
    ],
    "answer": "Change `i <= arr.length` to `i < arr.length`",
    "explanation": {
      "en": "The loop must stop before length to avoid reading undefined."
    }
  },
  {
    "id": 15,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "def average(nums):\n    total = sum(nums)\n    return total / len(num)",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `/` to `//`",
      "Change `len(num)` to `len(nums)`",
      "Change `sum(nums)` to `sum(num)`",
      "Return `total` only"
    ],
    "answer": "Change `len(num)` to `len(nums)`",
    "explanation": {
      "en": "The parameter is `nums`; `num` is undefined."
    }
  },
  {
    "id": 16,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "function double(arr) {\n  return arr.map(x => x * 2)\n}\nconsole.log(double(5));",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `x * 2` to `x + 2`",
      "Use `forEach` instead of `map`",
      "Pass an array like `double([5])`",
      "Remove the arrow function"
    ],
    "answer": "Pass an array like `double([5])`",
    "explanation": {
      "en": "`map` works on arrays, so pass `[5]` not the number 5."
    }
  },
  {
    "id": 17,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "def first_upper(words):\n    for w in words:\n        if w[0].isupper:\n            return w",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `w[0]` to `w[1]` in the test",
      "Use `w.isupper` on the whole word",
      "Return `words` instead of the count",
      "Change `isupper` to `isupper()`"
    ],
    "answer": "Change `isupper` to `isupper()`",
    "explanation": {
      "en": "`isupper` is a method and must be called with `()`."
    }
  },
  {
    "id": 18,
    "category": "typescript",
    "difficulty": "easy",
    "codeSnippet": "function getLen(s: string): number {\n  return s.length;\n}\nconst n: number = getLen(42);",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change return type to `string`",
      "Pass a string, e.g. `getLen(\"42\")`",
      "Change `s: string` to `s: any`",
      "Remove the type annotation on n"
    ],
    "answer": "Pass a string, e.g. `getLen(\"42\")`",
    "explanation": {
      "en": "The argument must be a string to match the parameter type."
    }
  },
  {
    "id": 19,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const obj = { a: 1, b: 2 };\nfor (const key in obj) {\n  console.log(obj.key);\n}",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `in` to `of`",
      "Use `Object.values(obj)`",
      "Change `obj.key` to `obj[key]`",
      "Change `const key` to `let key`"
    ],
    "answer": "Change `obj.key` to `obj[key]`",
    "explanation": {
      "en": "Dynamic property access needs bracket notation `obj[key]`."
    }
  },
  {
    "id": 20,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "def count_evens(nums):\n    c = 0\n    for n in nums:\n        if n % 2 == 1:\n            c += 1\n    return c",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `c += 1` to `c += n`",
      "Change `% 2` to `% 3`",
      "Start `c` at 1",
      "Change `== 1` to `== 0`"
    ],
    "answer": "Change `== 1` to `== 0`",
    "explanation": {
      "en": "Even numbers have remainder 0, so compare against 0."
    }
  },
  {
    "id": 21,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "function greet(name) {\n  return \"Hi \" + name;\n}\ngreet(\"Sam\")\nconsole.log(result);",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `+` to `,` in the greeting string",
      "Store the return: `const result = greet(\"Sam\")`",
      "Remove the return statement from greet",
      "Use `console.log(greet)` without calling"
    ],
    "answer": "Store the return: `const result = greet(\"Sam\")`",
    "explanation": {
      "en": "The result is never captured; assign the call to `result`."
    }
  },
  {
    "id": 22,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "def repeat(text, times):\n    return text * time",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `time` to `times`",
      "Change `*` to `+`",
      "Wrap with `str()`",
      "Swap the parameters"
    ],
    "answer": "Change `time` to `times`",
    "explanation": {
      "en": "The parameter is `times`; `time` is not defined."
    }
  },
  {
    "id": 23,
    "category": "typescript",
    "difficulty": "easy",
    "codeSnippet": "const ids: number[] = [];\nids.push(\"3\");",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `number[]` to `string[]`",
      "Push a number: `ids.push(3)`",
      "Use `ids.add(3)`",
      "Remove the type annotation"
    ],
    "answer": "Push a number: `ids.push(3)`",
    "explanation": {
      "en": "A `number[]` can only hold numbers, so push `3` not \"3\"."
    }
  },
  {
    "id": 24,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const max = Math.max([4, 9, 1]);",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Use `Math.maximum`",
      "Use `Math.max.apply([4,9,1])`",
      "Sort the array first",
      "Use `Math.max(...[4, 9, 1])`"
    ],
    "answer": "Use `Math.max(...[4, 9, 1])`",
    "explanation": {
      "en": "`Math.max` takes individual args; spread the array."
    }
  },
  {
    "id": 25,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "def safe_div(a, b):\n    if b == 0:\n        return None\n    return a / b\nprint(safe_div(10))",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Pass two args: `safe_div(10, 2)`",
      "Remove the `if` check",
      "Change `/` to `//`",
      "Return 0 instead of None"
    ],
    "answer": "Pass two args: `safe_div(10, 2)`",
    "explanation": {
      "en": "`safe_div` requires two arguments but only one was given."
    }
  },
  {
    "id": 26,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const s = \"racecar\";\nconst rev = s.split(\"\").reverse().join;",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `join` to `join(\"\")`",
      "Change `reverse()` to `reverse`",
      "Use `s.reverse()`",
      "Remove `split(\"\")`"
    ],
    "answer": "Change `join` to `join(\"\")`",
    "explanation": {
      "en": "`join` must be called with parentheses to produce a string."
    }
  },
  {
    "id": 27,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "const nums = [1, 2, 3, 4];\nnums.forEach((n, i) => {\n  if (n % 2 === 0) nums.splice(i, 1);\n});",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `splice` to `slice` in the loop",
      "Filter instead: `nums.filter(n => n % 2 !== 0)`",
      "Change `forEach` to `map` in the loop",
      "Use `i + 1` as the splice start index"
    ],
    "answer": "Filter instead: `nums.filter(n => n % 2 !== 0)`",
    "explanation": {
      "en": "Mutating an array while iterating skips elements; use filter."
    }
  },
  {
    "id": 28,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "def add_item(item, bag=[]):\n    bag.append(item)\n    return bag",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change default to `bag=None` and init inside",
      "Change `append` to `extend` in the body",
      "Return `item` instead of the bag list",
      "Remove the default value from the signature"
    ],
    "answer": "Change default to `bag=None` and init inside",
    "explanation": {
      "en": "A mutable default is shared across calls; use None then init."
    }
  },
  {
    "id": 29,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "async function getData() {\n  const res = fetch(\"/api\");\n  return res.json();\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Remove `async` from the function",
      "Change `res.json()` to `res.json`",
      "Add `await` before `fetch(\"/api\")`",
      "Wrap the body in `try/catch`"
    ],
    "answer": "Add `await` before `fetch(\"/api\")`",
    "explanation": {
      "en": "`fetch` returns a promise that must be awaited first."
    }
  },
  {
    "id": 30,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "var fns = [];\nfor (var i = 0; i < 3; i++) {\n  fns.push(() => i);\n}\nconsole.log(fns.map(f => f()));",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `() => i` to `() => i++`",
      "Use `fns[i] = i`",
      "Move the push outside the loop",
      "Change `var i` to `let i`"
    ],
    "answer": "Change `var i` to `let i`",
    "explanation": {
      "en": "`let` gives each iteration its own `i`; `var` shares one."
    }
  },
  {
    "id": 31,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "def dedupe(items):\n    seen = []\n    for x in items:\n        if x not in seen:\n            seen.append\n    return seen",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `not in` to `in` in the test",
      "Change `seen.append` to `seen.append(x)`",
      "Return `items` instead of the seen list",
      "Initialize `seen` as a set literal"
    ],
    "answer": "Change `seen.append` to `seen.append(x)`",
    "explanation": {
      "en": "`append` is referenced but never called with `x`."
    }
  },
  {
    "id": 32,
    "category": "typescript",
    "difficulty": "medium",
    "codeSnippet": "function findUser(id: number, users: {id: number}[]) {\n  return users.find(u => u.id = id);\n}",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `find` to `filter`",
      "Change `id: number` to `id: any`",
      "Change `u.id = id` to `u.id === id`",
      "Add a return type annotation"
    ],
    "answer": "Change `u.id = id` to `u.id === id`",
    "explanation": {
      "en": "The predicate assigns instead of comparing; use `===`."
    }
  },
  {
    "id": 33,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "function sum(arr) {\n  let total;\n  for (const n of arr) total += n;\n  return total;\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `of` to `in`",
      "Change `const n` to `let n`",
      "Return `arr.length`",
      "Initialize `total = 0`"
    ],
    "answer": "Initialize `total = 0`",
    "explanation": {
      "en": "`total` starts as undefined, so adding yields NaN; start at 0."
    }
  },
  {
    "id": 34,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "nums = [3, 1, 2]\nsorted_nums = nums.sort()\nprint(sorted_nums)",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Use `sorted_nums = sorted(nums)`",
      "Change `sort()` to `sort(reverse=True)`",
      "Print `nums.sort()` directly",
      "Use `nums.sorted()`"
    ],
    "answer": "Use `sorted_nums = sorted(nums)`",
    "explanation": {
      "en": "`list.sort()` returns None; `sorted()` returns a new list."
    }
  },
  {
    "id": 35,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "const counts = {};\nfor (const c of \"hello\") {\n  counts[c]++;\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `++` to `+= 2` in the body",
      "Change `of` to `in` in the loop",
      "Initialize `counts = []` as an array",
      "Use `counts[c] = (counts[c] || 0) + 1`"
    ],
    "answer": "Use `counts[c] = (counts[c] || 0) + 1`",
    "explanation": {
      "en": "Incrementing undefined gives NaN; default to 0 first."
    }
  },
  {
    "id": 36,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "def factorial(n):\n    if n == 0:\n        return 1\n    return n * factorial(n)",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `factorial(n)` to `factorial(n - 1)`",
      "Change the base case to `n == 1`",
      "Change `*` to `+` in the recursion",
      "Remove the base case from the function"
    ],
    "answer": "Change `factorial(n)` to `factorial(n - 1)`",
    "explanation": {
      "en": "The recursive call must shrink n, otherwise it never ends."
    }
  },
  {
    "id": 37,
    "category": "typescript",
    "difficulty": "medium",
    "codeSnippet": "function parseFlag(v?: string): boolean {\n  return v.toLowerCase() === \"true\";\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Guard: `return v?.toLowerCase() === \"true\"`",
      "Change the return type to `string`",
      "Remove the `?` from `v?` in the body",
      "Change `===` to `==` in the comparison"
    ],
    "answer": "Guard: `return v?.toLowerCase() === \"true\"`",
    "explanation": {
      "en": "`v` may be undefined; use optional chaining before calling."
    }
  },
  {
    "id": 38,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "function clone(obj) {\n  const copy = obj;\n  copy.x = 99;\n  return copy;\n}",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `copy.x` to `obj.x`",
      "Use `const copy = { ...obj }`",
      "Return `obj` instead",
      "Use `Object.keys(obj)`"
    ],
    "answer": "Use `const copy = { ...obj }`",
    "explanation": {
      "en": "Assigning shares the reference; spread to make a real copy."
    }
  },
  {
    "id": 39,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "function binarySearch(a, t) {\n  let lo = 0, hi = a.length;\n  while (lo < hi) {\n    const mid = (lo + hi) >> 1;\n    if (a[mid] === t) return mid;\n    if (a[mid] < t) lo = mid;\n    else hi = mid;\n  }\n  return -1;\n}",
    "question": {
      "en": "Which fix removes the subtle bug?"
    },
    "options": [
      "Change `hi = a.length` to `a.length - 1`",
      "Change `lo = mid` to `lo = mid + 1`",
      "Change `lo < hi` to `lo <= hi`",
      "Change `>> 1` to `/ 2`"
    ],
    "answer": "Change `lo = mid` to `lo = mid + 1`",
    "explanation": {
      "en": "Without `mid + 1`, lo can stall and loop forever."
    }
  },
  {
    "id": 40,
    "category": "python",
    "difficulty": "hard",
    "codeSnippet": "def chunk(lst, n):\n    result = []\n    for i in range(0, len(lst)):\n        result.append(lst[i:i + n])\n    return result",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `lst[i:i + n]` to `lst[i:n]` in body",
      "Change `append` to `extend` in the loop",
      "Start the range at 1 instead of 0 index",
      "Change `range(0, len(lst))` to `range(0, len(lst), n)`"
    ],
    "answer": "Change `range(0, len(lst))` to `range(0, len(lst), n)`",
    "explanation": {
      "en": "The step must be `n` so chunks do not overlap."
    }
  },
  {
    "id": 41,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "function debounce(fn, ms) {\n  let t;\n  return function (...args) {\n    clearTimeout(t);\n    t = setTimeout(fn(...args), ms);\n  };\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Remove `clearTimeout(t)` from the body",
      "Change to `setTimeout(() => fn(...args), ms)`",
      "Change `let t` to `const t` in the closure",
      "Move `clearTimeout` after setTimeout"
    ],
    "answer": "Change to `setTimeout(() => fn(...args), ms)`",
    "explanation": {
      "en": "`fn(...args)` runs immediately; pass a function instead."
    }
  },
  {
    "id": 42,
    "category": "typescript",
    "difficulty": "hard",
    "codeSnippet": "async function loadAll(urls: string[]) {\n  const out = [];\n  urls.forEach(async (u) => {\n    out.push(await fetch(u));\n  });\n  return out;\n}",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Remove `async` from the callback",
      "Change `forEach` to `map` only",
      "Add `await` before `forEach`",
      "Use `await Promise.all(urls.map(u => fetch(u)))`"
    ],
    "answer": "Use `await Promise.all(urls.map(u => fetch(u)))`",
    "explanation": {
      "en": "`forEach` does not await; collect promises with Promise.all."
    }
  },
  {
    "id": 43,
    "category": "python",
    "difficulty": "hard",
    "codeSnippet": "def merge(d1, d2):\n    merged = d1\n    for k, v in d2.items():\n        merged[k] = v\n    return merged",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `d2.items()` to `d2.keys()` in loop",
      "Use `merged.update(d1)` after the loop",
      "Change `merged = d1` to `merged = dict(d1)`",
      "Return `d2` instead of the merged dict"
    ],
    "answer": "Change `merged = d1` to `merged = dict(d1)`",
    "explanation": {
      "en": "`merged = d1` aliases d1; copy it so d1 is not mutated."
    }
  },
  {
    "id": 44,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "const obj = {\n  value: 10,\n  getValue: () => {\n    return this.value;\n  },\n};\nconsole.log(obj.getValue());",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Use a method shorthand `getValue() { return this.value; }`",
      "Change `this.value` to `obj.value` in the arrow",
      "Add `.bind(obj)` to the arrow function body",
      "Change `value: 10` to `let value = 10` above"
    ],
    "answer": "Use a method shorthand `getValue() { return this.value; }`",
    "explanation": {
      "en": "Arrow functions have no own `this`; use a normal method."
    }
  },
  {
    "id": 45,
    "category": "python",
    "difficulty": "hard",
    "codeSnippet": "def fib(n, memo={}):\n    if n <= 1:\n        return n\n    if n in memo:\n        return memo[n]\n    return fib(n - 1) + fib(n - 2)",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `n <= 1` to `n < 1` in the base case",
      "Change the default to `memo=None` and init",
      "Store result: `memo[n] = fib(n-1)+fib(n-2); return memo[n]`",
      "Remove the `if n in memo` cache check line"
    ],
    "answer": "Store result: `memo[n] = fib(n-1)+fib(n-2); return memo[n]`",
    "explanation": {
      "en": "Results are read from memo but never written, so it never caches."
    }
  },
  {
    "id": 46,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "function flatten(arr) {\n  return arr.reduce((acc, x) =>\n    Array.isArray(x) ? acc.concat(x) : acc.push(x), []);\n}",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change the initial value to `{}` instead",
      "Change `reduce` to `map` in the chain",
      "Remove the `Array.isArray(x)` check",
      "Replace `acc.push(x)` with `acc.concat(x)`"
    ],
    "answer": "Replace `acc.push(x)` with `acc.concat(x)`",
    "explanation": {
      "en": "`push` returns a length, not the accumulator; use concat."
    }
  },
  {
    "id": 47,
    "category": "typescript",
    "difficulty": "hard",
    "codeSnippet": "function uniq<T>(items: T[]): T[] {\n  const seen = new Set<T>();\n  return items.filter((x) => {\n    seen.add(x);\n    return !seen.has(x);\n  });\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `Set<T>` to `Array<T>` in the type",
      "Change `filter` to `map` in the pipeline",
      "Add `x` to seen twice inside the filter",
      "Return `!seen.has(x)` BEFORE calling `seen.add(x)`"
    ],
    "answer": "Return `!seen.has(x)` BEFORE calling `seen.add(x)`",
    "explanation": {
      "en": "Adding before checking makes `has` always true; check first."
    }
  },
  {
    "id": 48,
    "category": "python",
    "difficulty": "hard",
    "codeSnippet": "def remove_evens(nums):\n    for n in nums:\n        if n % 2 == 0:\n            nums.remove(n)\n    return nums",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `remove(n)` to `pop(n)`",
      "Change `== 0` to `!= 0`",
      "Iterate a copy: `for n in nums[:]:`",
      "Return a new empty list"
    ],
    "answer": "Iterate a copy: `for n in nums[:]:`",
    "explanation": {
      "en": "Removing while iterating skips items; loop over a copy."
    }
  },
  {
    "id": 49,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "function range(start, end) {\n  const out = [];\n  for (let i = start; i < end; i++) out.push(i);\n  return out;\n}\nconsole.log(range(1, 5).includes(5));",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Change `i < end` to `i <= end`",
      "Change `i = start` to `i = start - 1`",
      "Change `out.push(i)` to `out.push(i + 1)`",
      "Change `range(1, 5)` to `range(1, 4)`"
    ],
    "answer": "Change `i < end` to `i <= end`",
    "explanation": {
      "en": "To include `end`, the loop condition must be `i <= end`."
    }
  },
  {
    "id": 50,
    "category": "typescript",
    "difficulty": "hard",
    "codeSnippet": "function groupBy<T>(arr: T[], key: keyof T) {\n  const map: Record<string, T[]> = {};\n  for (const item of arr) {\n    const k = String(item[key]);\n    map[k].push(item);\n  }\n  return map;\n}",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Change `Record<string, T[]>` to `T[]` type",
      "Init the bucket: `(map[k] ??= []).push(item)`",
      "Change `of` to `in` in the for loop",
      "Change `String(item[key])` to `item[key]`"
    ],
    "answer": "Init the bucket: `(map[k] ??= []).push(item)`",
    "explanation": {
      "en": "The bucket array must exist before pushing into it."
    }
  },
  {
    "id": 51,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const nums = [10, 20, 30];\nfor (const i in nums) {\n  console.log(i * 2);\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Use `for (const n of nums)` to get values",
      "Use `for (const i on nums)` to get values",
      "Change `i * 2` to `i + i` in the body",
      "Change `const i` to `let i` in the header"
    ],
    "answer": "Use `for (const n of nums)` to get values",
    "explanation": {
      "en": "`for...in` yields string keys ('0','1'), so the multiply is wrong; `for...of` yields the values."
    }
  },
  {
    "id": 52,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "function half(s) {\n  return parseInt(s) / 2;\n}\nhalf('08');",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Pass the radix: `parseInt(s, 10)`",
      "Pass the radix: `parseInt(s, 2)`",
      "Swap `parseInt` for `parseFloat(s)`",
      "Wrap the whole call in `String(s)`"
    ],
    "answer": "Pass the radix: `parseInt(s, 10)`",
    "explanation": {
      "en": "Always give parseInt a radix; without it the base is implementation-dependent for odd input."
    }
  },
  {
    "id": 53,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "def greet(self, name):\n    return \"Hi \" + name\n\nclass Greeter:\n    greet = greet",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Define `greet` inside the class body",
      "Define `greet` after the class body",
      "Rename the `self` parameter to `cls`",
      "Return `name` without the greeting"
    ],
    "answer": "Define `greet` inside the class body",
    "explanation": {
      "en": "A method belongs in the class body; assigning a stray function is fragile and confusing."
    }
  },
  {
    "id": 54,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "switch (kind) {\n  case 'a':\n    total += 1;\n  case 'b':\n    total += 10;\n}",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Add `break;` at the end of each case",
      "Add `return;` at the end of each case",
      "Change `switch` to a chain of `if`s",
      "Move `case 'b'` above `case 'a'`"
    ],
    "answer": "Add `break;` at the end of each case",
    "explanation": {
      "en": "Without break the cases fall through, so 'a' also runs the 'b' branch."
    }
  },
  {
    "id": 55,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "s = \"hello\"\ns[0] = \"H\"\nprint(s)",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Rebuild it: `s = \"H\" + s[1:]`",
      "Rebuild it: `s = \"H\" + s[0:]`",
      "Convert first with `s = list(s)`",
      "Use `s.replace(0, \"H\")` instead"
    ],
    "answer": "Rebuild it: `s = \"H\" + s[1:]`",
    "explanation": {
      "en": "Python strings are immutable, so build a new string instead of assigning to an index."
    }
  },
  {
    "id": 56,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const items = [3, 1, 2];\nconst sorted = items.sort();\nconsole.log(items);",
    "question": {
      "en": "Which fix removes the subtle bug?"
    },
    "options": [
      "Copy first: `[...items].sort((a,b)=>a-b)`",
      "Copy first: `[...items].reverse((a,b)=>a-b)`",
      "Assign it back with `items = items.sort()`",
      "Call `items.sort()` a second time after"
    ],
    "answer": "Copy first: `[...items].sort((a,b)=>a-b)`",
    "explanation": {
      "en": "sort() mutates the original array in place; spread into a copy to leave `items` untouched."
    }
  },
  {
    "id": 57,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "a = [1, 2, 3]\nb = a\nb.append(4)\nprint(len(a))",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Copy it with `b = a[:]` instead",
      "Copy it with `b = a[0]` instead",
      "Change `b.append(4)` to `b + [4]`",
      "Declare `b` before `a` is created"
    ],
    "answer": "Copy it with `b = a[:]` instead",
    "explanation": {
      "en": "`b = a` binds the same list, so appending is visible through `a`; slice to copy it."
    }
  },
  {
    "id": 58,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const found = [1, 2, 3].forEach((n) => {\n  if (n === 2) return n;\n});",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Use `find` so the match is returned",
      "Use `filter` so the match is returned",
      "Add `return` before the `forEach` call",
      "Change the arrow to a normal function"
    ],
    "answer": "Use `find` so the match is returned",
    "explanation": {
      "en": "forEach always returns undefined and a return inside it only exits that callback; find returns the match."
    }
  },
  {
    "id": 59,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "counts = {\"a\": 1, \"b\": 0}\nfor k in counts:\n    if counts[k] == 0:\n        del counts[k]",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Iterate a copy: `for k in list(counts)`",
      "Iterate a copy: `for k in dict(counts[k])`",
      "Change `del counts[k]` to `counts.pop()`",
      "Change the test to `if not counts[k]:`"
    ],
    "answer": "Iterate a copy: `for k in list(counts)`",
    "explanation": {
      "en": "Deleting while iterating a dict raises RuntimeError; iterate over a snapshot of the keys."
    }
  },
  {
    "id": 60,
    "category": "typescript",
    "difficulty": "medium",
    "codeSnippet": "async function total(): Promise<number> {\n  const res = fetch('/api/sum');\n  return (await res).ok ? 1 : 0;\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Await at the call: `await fetch('/api/sum')`",
      "Await at the call: `async fetch('/api/sum')`",
      "Drop `async` from the function signature",
      "Change the return type to `Promise<void>`"
    ],
    "answer": "Await at the call: `await fetch('/api/sum')`",
    "explanation": {
      "en": "Await the promise where it is created so `res` is the response, not a pending promise."
    }
  },
  {
    "id": 61,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "const obj = {\n  count: 0,\n  start() {\n    setTimeout(function () {\n      this.count++;\n    }, 10);\n  }\n};",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Use an arrow callback so `this` is kept",
      "Use a normal callback so `this` is kept",
      "Change `this.count++` to `count++` there",
      "Move `setTimeout` outside of the method"
    ],
    "answer": "Use an arrow callback so `this` is kept",
    "explanation": {
      "en": "A normal function gets its own `this`; an arrow keeps the enclosing method's `this`."
    }
  },
  {
    "id": 62,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "def rate(a, b):\n    if b is 0:\n        return 0\n    return a / b",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Compare by value with `if b == 0:`",
      "Compare by value with `if b = 0:`",
      "Change the guard to `if not b is 0:`",
      "Return `None` instead of 0 when empty"
    ],
    "answer": "Compare by value with `if b == 0:`",
    "explanation": {
      "en": "`is` compares identity, not value; use == for numeric comparison."
    }
  },
  {
    "id": 63,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "function tag(name) {\n  this.name = name;\n}\nconst t = tag('box');\nconsole.log(t.name);",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Construct it: `const t = new tag('box')`",
      "Construct it: `const t = call tag('box')`",
      "Add `return this;` at the end of `tag`",
      "Rename the parameter from `name` to `n`"
    ],
    "answer": "Construct it: `const t = new tag('box')`",
    "explanation": {
      "en": "Without `new` the call has no fresh `this`, so it returns undefined."
    }
  },
  {
    "id": 64,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "try:\n    value = int(raw)\nexcept:\n    value = 0",
    "question": {
      "en": "Which fix removes the subtle bug?"
    },
    "options": [
      "Catch narrowly: `except ValueError:`",
      "Catch narrowly: `except Exception as:`",
      "Move the assignment above the `try`",
      "Add an `else:` branch after the except"
    ],
    "answer": "Catch narrowly: `except ValueError:`",
    "explanation": {
      "en": "A bare except also swallows KeyboardInterrupt and real errors; catch the specific one."
    }
  },
  {
    "id": 65,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "const a = [1, 2];\nconst b = [1, 2];\nif (a === b) {\n  console.log('same');\n}",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Compare contents element by element",
      "Compare contents with a loose `==`",
      "Freeze both arrays before comparing",
      "Convert both with `Number(a)` first"
    ],
    "answer": "Compare contents element by element",
    "explanation": {
      "en": "=== compares references for arrays, so two distinct arrays are never equal; compare contents."
    }
  },
  {
    "id": 66,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "def top(words):\n    return sorted(words, key=len(words))",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Pass the function itself: `key=len`",
      "Pass the function itself: `key=len()`",
      "Change `sorted` to `words.sort(len)`",
      "Add `reverse=True` to the sorted call"
    ],
    "answer": "Pass the function itself: `key=len`",
    "explanation": {
      "en": "key takes a function to call per item; `len(words)` passes a number instead."
    }
  },
  {
    "id": 67,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "let total = 0;\n[1, 2, 3].forEach((n) => {\n  total = total + n;\n});\nconst avg = total / 0;",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Divide by the count: `total / 3`",
      "Divide by the count: `total / n`",
      "Move `avg` inside the forEach body",
      "Change `let total` to `const total`"
    ],
    "answer": "Divide by the count: `total / 3`",
    "explanation": {
      "en": "Dividing by zero gives Infinity; divide by the number of elements."
    }
  },
  {
    "id": 68,
    "category": "typescript",
    "difficulty": "medium",
    "codeSnippet": "const seen: Map<string, number> = new Map();\nseen['a'] = 1;\nconsole.log(seen.size);",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Store it with `seen.set('a', 1)`",
      "Store it with `seen.push('a', 1)`",
      "Change `Map` to a plain object type",
      "Read the count with `seen.length`"
    ],
    "answer": "Store it with `seen.set('a', 1)`",
    "explanation": {
      "en": "Bracket assignment adds a plain property and never touches the Map, so size stays 0."
    }
  },
  {
    "id": 69,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "total = 0\nfor row in rows:\n    total += row\nprint(\"total: \" + total)",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Convert it: `\"total: \" + str(total)`",
      "Convert it: `\"total: \" + int(total)`",
      "Change `total = 0` to `total = \"\"`",
      "Use `print(\"total: \", + total)` here"
    ],
    "answer": "Convert it: `\"total: \" + str(total)`",
    "explanation": {
      "en": "A str and an int cannot be concatenated; convert the number with str()."
    }
  },
  {
    "id": 70,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "const cfg = { retries: 0 };\nconst retries = cfg.retries || 3;\nconsole.log(retries);",
    "question": {
      "en": "Which fix removes the subtle bug?"
    },
    "options": [
      "Use `??` so only null/undefined default",
      "Use `&&` so only null/undefined default",
      "Change `retries: 0` to `retries: null`",
      "Compare explicitly with `cfg.retries > 0`"
    ],
    "answer": "Use `??` so only null/undefined default",
    "explanation": {
      "en": "0 is falsy so || replaces a valid value; ?? only falls back on null or undefined."
    }
  },
  {
    "id": 71,
    "category": "python",
    "difficulty": "hard",
    "codeSnippet": "grid = [[0] * 3] * 3\ngrid[0][0] = 5\nprint(grid[1][0])",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Build rows: `[[0] * 3 for _ in range(3)]`",
      "Build rows: `[[0] for _ in range(3) * 3]`",
      "Change `grid[0][0] = 5` to append a row",
      "Wrap the outer list in `list(grid)` first"
    ],
    "answer": "Build rows: `[[0] * 3 for _ in range(3)]`",
    "explanation": {
      "en": "`* 3` repeats the SAME inner list three times, so one write appears in every row."
    }
  },
  {
    "id": 72,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "async function all(ids) {\n  const out = [];\n  ids.forEach(async (id) => {\n    out.push(await load(id));\n  });\n  return out;\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Use `await Promise.all(ids.map(load))`",
      "Use `await Promise.race(ids.map(load))`",
      "Add `await` before the `ids.forEach` call",
      "Change `out.push` to `out.concat` inside"
    ],
    "answer": "Use `await Promise.all(ids.map(load))`",
    "explanation": {
      "en": "forEach ignores the async callbacks, so `out` is still empty at the return; Promise.all waits."
    }
  },
  {
    "id": 73,
    "category": "python",
    "difficulty": "hard",
    "codeSnippet": "fns = []\nfor i in range(3):\n    fns.append(lambda: i)\nprint(fns[0]())",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Bind it now: `lambda i=i: i` in append",
      "Bind it now: `lambda i: i` in the append",
      "Move the append above the `for` header",
      "Change `range(3)` to `list(range(3))`"
    ],
    "answer": "Bind it now: `lambda i=i: i` in append",
    "explanation": {
      "en": "The lambdas close over the variable, not its value, so all see the final i; a default binds it."
    }
  },
  {
    "id": 74,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "const clone = { ...user };\nclone.address.city = 'Riyadh';\nconsole.log(user.address.city);",
    "question": {
      "en": "Which fix removes the subtle bug?"
    },
    "options": [
      "Deep copy: `structuredClone(user)` first",
      "Deep copy: `Object.freeze(user)` first",
      "Spread it twice to reach nested values",
      "Assign with `clone.address = 'Riyadh'`"
    ],
    "answer": "Deep copy: `structuredClone(user)` first",
    "explanation": {
      "en": "Spread copies one level, so `address` is shared and the write leaks into `user`."
    }
  },
  {
    "id": 75,
    "category": "typescript",
    "difficulty": "hard",
    "codeSnippet": "function pick<T>(list: T[], i: number): T {\n  return list[i];\n}\nconst v = pick([1, 2], 9);",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Return `T | undefined` and guard the index",
      "Return `T | never` and guard the index",
      "Change the parameter type to `i: T`",
      "Mark the whole function as `async`"
    ],
    "answer": "Return `T | undefined` and guard the index",
    "explanation": {
      "en": "An out-of-range index yields undefined, which the declared return type wrongly denies."
    }
  },
  {
    "id": 76,
    "category": "python",
    "difficulty": "hard",
    "codeSnippet": "def add_tag(tag, tags=[]):\n    tags.append(tag)\n    return tags\n\nprint(add_tag(\"a\"), add_tag(\"b\"))",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Default to `None` and build inside",
      "Default to `list` and build inside",
      "Return `tags[:]` at the end instead",
      "Move `tags.append(tag)` above the def"
    ],
    "answer": "Default to `None` and build inside",
    "explanation": {
      "en": "A mutable default is created once and shared by every call, so the list keeps growing."
    }
  },
  {
    "id": 77,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "const ids = rows.map((r) => r.id);\nconst uniq = ids.filter((id, i) => ids.indexOf(id) === i);\nconst missing = uniq.filter((id) => !id);",
    "question": {
      "en": "Which fix removes the subtle bug?"
    },
    "options": [
      "Test with `id == null` so 0 is kept",
      "Test with `id === null` so 0 is kept",
      "Change `uniq.filter` to `uniq.every`",
      "Compare `indexOf(id) !== i` instead"
    ],
    "answer": "Test with `id == null` so 0 is kept",
    "explanation": {
      "en": "`!id` also matches the valid id 0 and an empty string; test for null/undefined explicitly."
    }
  },
  {
    "id": 78,
    "category": "python",
    "difficulty": "hard",
    "codeSnippet": "with open(path) as f:\n    data = f.read()\nprint(f.closed)\nrows = data.split(\"\\n\")[1:]",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Strip first so a trailing blank line goes",
      "Strip first so a leading header line goes",
      "Read the file with `f.readlines()` here",
      "Move `rows = ...` inside the with block"
    ],
    "answer": "Strip first so a trailing blank line goes",
    "explanation": {
      "en": "A file usually ends with a newline, so split leaves an empty final row; strip it first."
    }
  },
  {
    "id": 79,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "let timer;\nfunction poll() {\n  timer = setInterval(fetchNow, 1000);\n}\npoll();\npoll();",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Call `clearInterval(timer)` before setting",
      "Call `clearTimeout(timer)` before setting",
      "Change `setInterval` to `setTimeout` here",
      "Declare `timer` inside the `poll` function"
    ],
    "answer": "Call `clearInterval(timer)` before setting",
    "explanation": {
      "en": "The second call orphans the first interval, which keeps firing forever; clear it first."
    }
  },
  {
    "id": 80,
    "category": "typescript",
    "difficulty": "hard",
    "codeSnippet": "enum Role { User, Admin }\nfunction isAdmin(r: Role) {\n  return r == Role.Admin || r;\n}",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Return `r === Role.Admin` on its own",
      "Return `r !== Role.Admin` on its own",
      "Change the enum so `User` starts at 1",
      "Compare against the string `'Admin'`"
    ],
    "answer": "Return `r === Role.Admin` on its own",
    "explanation": {
      "en": "The trailing `|| r` makes any non-zero role truthy; the comparison alone is the answer."
    }
  },
  {
    "id": 81,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const rows = Array(3).fill([]);\nrows[0].push('a');\nconsole.log(rows[1].length);",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Build each row: `Array.from({length:3}, () => [])`",
      "Build each row: `Array.from({length:3}, [])`",
      "Change `fill([])` to `fill(new Array())`",
      "Push with `rows[0] = rows[0].concat('a')`"
    ],
    "answer": "Build each row: `Array.from({length:3}, () => [])`",
    "explanation": {
      "en": "fill puts the SAME array in every slot, so one push shows up in all rows."
    }
  },
  {
    "id": 82,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const nums = [5, 1, 10];\nnums.sort((a, b) => a > b);",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Return a number: `(a, b) => a - b`",
      "Return a number: `(a, b) => a < b`",
      "Drop the comparator and call `sort()`",
      "Wrap the result in `Number(a > b)`"
    ],
    "answer": "Return a number: `(a, b) => a - b`",
    "explanation": {
      "en": "A comparator must return a negative, zero or positive number, not a boolean."
    }
  },
  {
    "id": 83,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const price = 19.99;\nconst label = price.toFixed(2) + 5;\nconsole.log(label);",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Add first: `(price + 5).toFixed(2)` instead",
      "Add first: `(price + 5).toString(2)` instead",
      "Change `+ 5` to `+ '5'` in the line",
      "Call `parseInt` on the label afterwards"
    ],
    "answer": "Add first: `(price + 5).toFixed(2)` instead",
    "explanation": {
      "en": "toFixed returns a string, so + 5 concatenates instead of adding."
    }
  },
  {
    "id": 84,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const list = ['a', 'b'];\nif ('a' in list) {\n  console.log('found');\n}",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Test membership with `list.includes('a')`",
      "Test membership with `list.contains('a')`",
      "Change the array into a plain object",
      "Compare directly with `list == 'a'`"
    ],
    "answer": "Test membership with `list.includes('a')`",
    "explanation": {
      "en": "`in` tests keys ('0','1') on an array, not the values stored in it."
    }
  },
  {
    "id": 85,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "def total(items):\n    sum = 0\n    for i in items:\n        sum += i\n    return sum(items)",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Return the accumulator, not `sum(items)`",
      "Return the accumulator, not `items.sum()`",
      "Rename the loop variable from `i` to `n`",
      "Initialise `sum` to None before the loop"
    ],
    "answer": "Return the accumulator, not `sum(items)`",
    "explanation": {
      "en": "The local `sum` shadows the builtin, so calling it raises TypeError."
    }
  },
  {
    "id": 86,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "count = 0\ndef bump():\n    count += 1\nbump()",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Declare `global count` inside the function",
      "Declare `nonlocal count` inside the function",
      "Change `count += 1` to `count = count + 1`",
      "Move the `count = 0` line into `bump`"
    ],
    "answer": "Declare `global count` inside the function",
    "explanation": {
      "en": "Assigning makes `count` local; `global` binds it to the module-level name."
    }
  },
  {
    "id": 87,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "cfg = {'host': 'x'}\nport = cfg['port']",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Read it safely: `cfg.get('port', 80)`",
      "Read it safely: `cfg.fetch('port', 80)`",
      "Wrap the dict in `list(cfg)` first",
      "Change the key to `cfg['Port']` here"
    ],
    "answer": "Read it safely: `cfg.get('port', 80)`",
    "explanation": {
      "en": "Bracket access raises KeyError for a missing key; get() returns a default."
    }
  },
  {
    "id": 88,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const raw = '{bad json}';\nconst cfg = JSON.parse(raw);",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Wrap the parse in a `try/catch` block",
      "Wrap the parse in a `try/finally` block",
      "Call `JSON.stringify(raw)` before parsing",
      "Compare `raw` against an empty string"
    ],
    "answer": "Wrap the parse in a `try/catch` block",
    "explanation": {
      "en": "JSON.parse throws on malformed input; catch it instead of crashing."
    }
  },
  {
    "id": 89,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const name = ' Sara ';\nif (name === 'Sara') {\n  greet();\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Compare `name.trim() === 'Sara'` instead",
      "Compare `name.trimAll() === 'Sara'` instead",
      "Change `===` to `==` in the condition",
      "Wrap the name in `String(name)` first"
    ],
    "answer": "Compare `name.trim() === 'Sara'` instead",
    "explanation": {
      "en": "The surrounding spaces make the strings differ; trim before comparing."
    }
  },
  {
    "id": 90,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "names = ['ana', 'bob']\nfor i in range(1, len(names)):\n    print(names[i])",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Start at zero: `range(0, len(names))`",
      "Start at zero: `range(0, len(names) - 1)`",
      "Change `names[i]` to `names[i - 1]`",
      "Iterate with `for i in names` instead"
    ],
    "answer": "Start at zero: `range(0, len(names))`",
    "explanation": {
      "en": "Indexes start at 0, so starting at 1 silently skips the first name."
    }
  },
  {
    "id": 91,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "class Counter {\n  n = 0;\n  inc() { this.n++; }\n}\nbtn.addEventListener('click', new Counter().inc);",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Wrap it: `() => counter.inc()` as handler",
      "Wrap it: `() => Counter.inc()` as handler",
      "Change `inc()` to a static class method",
      "Change `n = 0` to `this.n = 0` in class"
    ],
    "answer": "Wrap it: `() => counter.inc()` as handler",
    "explanation": {
      "en": "Passing the method loses its receiver, so `this` is undefined when it runs."
    }
  },
  {
    "id": 92,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "const users = {};\nif (users.hasOwnProperty('toString')) {\n  console.log('custom');\n}",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Use `Object.hasOwn(users, 'toString')`",
      "Use `Object.hasKey(users, 'toString')`",
      "Change the test to `'toString' in users`",
      "Compare `users.toString !== undefined`"
    ],
    "answer": "Use `Object.hasOwn(users, 'toString')`",
    "explanation": {
      "en": "hasOwnProperty can be shadowed by a key of the same name; Object.hasOwn is safe."
    }
  },
  {
    "id": 93,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "const merged = Object.assign({}, base);\nmerged.opts.debug = true;\nconsole.log(base.opts.debug);",
    "question": {
      "en": "Which fix removes the subtle bug?"
    },
    "options": [
      "Copy deeply with `structuredClone(base)`",
      "Copy deeply with `Object.entries(base)`",
      "Call `Object.assign` with two sources",
      "Freeze `base` before the assignment"
    ],
    "answer": "Copy deeply with `structuredClone(base)`",
    "explanation": {
      "en": "Object.assign copies one level, so `opts` is shared and the write leaks into base."
    }
  },
  {
    "id": 94,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "try:\n    run()\nexcept Exception:\n    log('general')\nexcept ValueError:\n    log('value')",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Put `except ValueError` before Exception",
      "Put `except ValueError` after the finally",
      "Merge them into one `except` statement",
      "Change `Exception` to `BaseException`"
    ],
    "answer": "Put `except ValueError` before Exception",
    "explanation": {
      "en": "The first matching handler wins, so the broad one makes the specific one unreachable."
    }
  },
  {
    "id": 95,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "import copy\na = {'x': [1, 2]}\nb = copy.copy(a)\nb['x'].append(3)\nprint(len(a['x']))",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Use `copy.deepcopy(a)` for the copy",
      "Use `copy.clone(a)` for the copy",
      "Change `b['x']` to `b.get('x')` here",
      "Append with `b['x'] = b['x'] + [3]`"
    ],
    "answer": "Use `copy.deepcopy(a)` for the copy",
    "explanation": {
      "en": "A shallow copy shares the inner list, so appending is visible through `a`."
    }
  },
  {
    "id": 96,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "const ok = [1, 2, 3].reduce((a, b) => a + b);\nconst bad = [].reduce((a, b) => a + b);",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Give a seed: `.reduce((a, b) => a + b, 0)`",
      "Give a seed: `.reduce(0, (a, b) => a + b)`",
      "Guard with `if (arr.length > 1)` first",
      "Change `reduce` to `reduceRight` here"
    ],
    "answer": "Give a seed: `.reduce((a, b) => a + b, 0)`",
    "explanation": {
      "en": "reduce on an empty array without an initial value throws a TypeError."
    }
  },
  {
    "id": 97,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "async function loadAll(ids) {\n  const out = [];\n  for (const id of ids) {\n    out.push(await fetchOne(id));\n  }\n  return out;\n}",
    "question": {
      "en": "Which fix removes the inefficiency?"
    },
    "options": [
      "Run them together with `Promise.all(map)`",
      "Run them together with `Promise.any(map)`",
      "Remove the `await` from inside the loop",
      "Change the `for...of` into a `forEach`"
    ],
    "answer": "Run them together with `Promise.all(map)`",
    "explanation": {
      "en": "Awaiting inside the loop serialises the requests; Promise.all runs them in parallel."
    }
  },
  {
    "id": 98,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "fetchUser().then((u) => render(u));",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Add a `.catch((e) => report(e))` handler",
      "Add a `.finally((e) => report(e))` handler",
      "Wrap the call in a `try/catch` block",
      "Change `.then` into an `await` call"
    ],
    "answer": "Add a `.catch((e) => report(e))` handler",
    "explanation": {
      "en": "Without catch a rejection becomes an unhandled promise rejection."
    }
  },
  {
    "id": 99,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "def price(v):\n    assert v > 0, 'must be positive'\n    return v * 2",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Raise explicitly: `if v <= 0: raise ValueError`",
      "Raise explicitly: `if v <= 0: return ValueError`",
      "Change the assert message to be clearer",
      "Move the assert below the return line"
    ],
    "answer": "Raise explicitly: `if v <= 0: raise ValueError`",
    "explanation": {
      "en": "Assertions are stripped when Python runs with -O, so the check silently disappears."
    }
  },
  {
    "id": 100,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "out = ''\nfor row in rows:\n    out += row + ','",
    "question": {
      "en": "Which fix removes the inefficiency?"
    },
    "options": [
      "Build once with `','.join(rows)` instead",
      "Build once with `','.append(rows)` instead",
      "Change `out = ''` to `out = []` only",
      "Use `out = out + row` inside the loop"
    ],
    "answer": "Build once with `','.join(rows)` instead",
    "explanation": {
      "en": "Strings are immutable, so += rebuilds the whole string each pass; join builds it once."
    }
  },
  {
    "id": 101,
    "category": "typescript",
    "difficulty": "medium",
    "codeSnippet": "function len(v: any) {\n  return v.length;\n}\nlen(42);",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Type it as `string | unknown[]` instead",
      "Type it as `object | unknown[]` instead",
      "Add a `// @ts-ignore` above the call",
      "Return `v?.length` to guard the access"
    ],
    "answer": "Type it as `string | unknown[]` instead",
    "explanation": {
      "en": "`any` disables checking entirely, so a number slips through; narrow the parameter type."
    }
  },
  {
    "id": 102,
    "category": "typescript",
    "difficulty": "medium",
    "codeSnippet": "const el = document.querySelector('.box')!;\nel.classList.add('on');",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Guard it: `if (el) el.classList.add('on')`",
      "Guard it: `if (el!) el.classList.add('on')`",
      "Change `querySelector` to `getElementById`",
      "Cast the result to `as HTMLElement` first"
    ],
    "answer": "Guard it: `if (el) el.classList.add('on')`",
    "explanation": {
      "en": "The ! only silences the type error; at runtime a missing element still throws."
    }
  },
  {
    "id": 103,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "const page = 2, size = 10;\nconst rows = all.slice(page * size, page * size + size);",
    "question": {
      "en": "Which fix removes the off-by-one bug?"
    },
    "options": [
      "Offset from zero: `(page - 1) * size`",
      "Offset from zero: `(page - 1) + size`",
      "Change `slice` into `splice` for paging",
      "Add one to the end: `page * size + 1`"
    ],
    "answer": "Offset from zero: `(page - 1) * size`",
    "explanation": {
      "en": "With 1-based page numbers, page 2 must start at offset 10, not 20 — a whole page is skipped."
    }
  },
  {
    "id": 104,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "const versions = ['1.10.0', '1.9.0'];\nversions.sort();\nconsole.log(versions[0]);",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Compare each numeric part separately",
      "Compare with `localeCompare` directly",
      "Pad every version to a fixed length",
      "Reverse the array after sorting it"
    ],
    "answer": "Compare each numeric part separately",
    "explanation": {
      "en": "String order puts '1.10.0' before '1.9.0'; compare the numbers segment by segment."
    }
  },
  {
    "id": 105,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "if user.get('role') is 'admin':\n    grant()",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Compare by value: `== 'admin'` instead",
      "Compare by value: `in 'admin'` instead",
      "Change `.get('role')` to `['role']`",
      "Wrap the value in `str()` before the test"
    ],
    "answer": "Compare by value: `== 'admin'` instead",
    "explanation": {
      "en": "`is` compares object identity, which is unreliable for strings built at runtime."
    }
  },
  {
    "id": 106,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "for i in range(0, 1, 0.1):\n    print(i)",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Scale to ints: `range(0, 10)` then divide",
      "Scale to ints: `range(0, 10, 0.1)` then divide",
      "Change `range` to `enumerate` in the loop",
      "Cast the step with `int(0.1)` beforehand"
    ],
    "answer": "Scale to ints: `range(0, 10)` then divide",
    "explanation": {
      "en": "range only accepts integers; iterate whole numbers and divide inside the loop."
    }
  },
  {
    "id": 107,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "const emails = rows.map((r) => r.email.toLowerCase());",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Guard it: `r.email?.toLowerCase() ?? ''`",
      "Guard it: `r.email!.toLowerCase() ?? ''`",
      "Filter the rows after the map call",
      "Change `map` into a `forEach` loop"
    ],
    "answer": "Guard it: `r.email?.toLowerCase() ?? ''`",
    "explanation": {
      "en": "One row without an email throws and kills the whole map; guard the access."
    }
  },
  {
    "id": 108,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "const cache = {};\nfunction get(key) {\n  if (cache[key]) return cache[key];\n  cache[key] = compute(key);\n  return cache[key];\n}",
    "question": {
      "en": "Which fix removes the subtle bug?"
    },
    "options": [
      "Test presence with `key in cache` instead",
      "Test presence with `cache[key] != null`",
      "Initialise the cache as `new Set()` first",
      "Return `cache[key] || compute(key)` only"
    ],
    "answer": "Test presence with `key in cache` instead",
    "explanation": {
      "en": "A cached 0, '' or false is falsy, so the value is recomputed every call."
    }
  },
  {
    "id": 109,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "let seq = 0;\nasync function next() {\n  const v = seq;\n  await tick();\n  seq = v + 1;\n  return seq;\n}",
    "question": {
      "en": "Which fix removes the race?"
    },
    "options": [
      "Increment before awaiting: `seq += 1` first",
      "Increment before awaiting: `seq == 1` first",
      "Wrap the body in a `try/finally` block",
      "Change `let seq` to `const seq` at the top"
    ],
    "answer": "Increment before awaiting: `seq += 1` first",
    "explanation": {
      "en": "Two concurrent calls both read the old value across the await and write the same number."
    }
  },
  {
    "id": 110,
    "category": "python",
    "difficulty": "hard",
    "codeSnippet": "class Node:\n    children = []\n    def add(self, c):\n        self.children.append(c)",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Create it per instance in `__init__`",
      "Create it per instance in `__new__`",
      "Change `append` to `self.children + [c]`",
      "Mark `children` as a `@property` value"
    ],
    "answer": "Create it per instance in `__init__`",
    "explanation": {
      "en": "A class attribute is shared by every instance, so all nodes get the same children list."
    }
  },
  {
    "id": 111,
    "category": "python",
    "difficulty": "hard",
    "codeSnippet": "def walk(node, seen=set()):\n    seen.add(node.id)\n    for c in node.children:\n        walk(c, seen)",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Default to `None` and create it inside",
      "Default to `set` and create it inside",
      "Return `seen` at the end of the walk",
      "Change `seen.add` to `seen.update`"
    ],
    "answer": "Default to `None` and create it inside",
    "explanation": {
      "en": "The default set is created once, so a second top-level walk still sees the first one's ids."
    }
  },
  {
    "id": 112,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "el.addEventListener('scroll', () => heavy());\n// later\nel.removeEventListener('scroll', () => heavy());",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Keep one named handler and remove that",
      "Keep one inline arrow and remove that",
      "Pass `{ once: true }` to the remove call",
      "Call `el.removeEventListener('scroll')`"
    ],
    "answer": "Keep one named handler and remove that",
    "explanation": {
      "en": "The two arrows are different function objects, so the listener is never removed."
    }
  },
  {
    "id": 113,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "const total = items.reduce((sum, i) => sum + i.price, 0);\nconst cents = total * 100;\nsave(Math.round(cents));",
    "question": {
      "en": "Which fix removes the subtle bug?"
    },
    "options": [
      "Round each price to cents before summing",
      "Round each price to cents after summing",
      "Store the total as a fixed-2 string value",
      "Replace `Math.round` with `Math.floor`"
    ],
    "answer": "Round each price to cents before summing",
    "explanation": {
      "en": "Summing binary floats accumulates error; convert to integer cents first and add those."
    }
  },
  {
    "id": 114,
    "category": "python",
    "difficulty": "hard",
    "codeSnippet": "rows = [r for r in rows if r.ok]\nfor i, r in enumerate(rows):\n    if not r.keep:\n        del rows[i]",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Rebuild it: `[r for r in rows if r.keep]`",
      "Rebuild it: `[r for r in rows if not r.keep]`",
      "Iterate backwards over the row indexes",
      "Change `del rows[i]` to `rows.pop()`"
    ],
    "answer": "Rebuild it: `[r for r in rows if r.keep]`",
    "explanation": {
      "en": "Deleting during enumerate shifts later items and skips them; build a new list instead."
    }
  },
  {
    "id": 115,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "const idx = {};\nfor (const r of rows) idx[r.id] = r;\nconst ordered = Object.keys(idx);",
    "question": {
      "en": "Which fix removes the subtle bug?"
    },
    "options": [
      "Use a `Map` to keep insertion order",
      "Use a `Set` to keep insertion order",
      "Sort the keys after collecting them",
      "Convert every id to a string first"
    ],
    "answer": "Use a `Map` to keep insertion order",
    "explanation": {
      "en": "Integer-like object keys are ordered numerically, not by insertion; a Map preserves order."
    }
  },
  {
    "id": 116,
    "category": "typescript",
    "difficulty": "hard",
    "codeSnippet": "interface Opts { retries?: number }\nfunction run(o: Opts) {\n  const r = o.retries || 3;\n  return r;\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Default with `o.retries ?? 3` instead",
      "Default with `o.retries && 3` instead",
      "Make `retries` required in the interface",
      "Change the return type to `number | undefined`"
    ],
    "answer": "Default with `o.retries ?? 3` instead",
    "explanation": {
      "en": "An explicit 0 is falsy and gets replaced by 3; ?? only defaults on null or undefined."
    }
  },
  {
    "id": 117,
    "category": "python",
    "difficulty": "hard",
    "codeSnippet": "import re\nif re.match(r'\\d+', value):\n    accept(value)",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Anchor the whole value: `r'\\d+$'`",
      "Anchor the whole value: `r'^\\d+'`",
      "Change `re.match` to `re.search` here",
      "Compile the pattern before matching it"
    ],
    "answer": "Anchor the whole value: `r'\\d+$'`",
    "explanation": {
      "en": "match only anchors the start, so '12abc' is accepted; add $ to require the full string."
    }
  },
  {
    "id": 118,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "const first = 'e\\u0301'.charAt(0);\nconsole.log('🙂'.length);",
    "question": {
      "en": "Which fix handles the text correctly?"
    },
    "options": [
      "Iterate code points: `[...str]` instead",
      "Iterate code points: `str.split('')` instead",
      "Call `normalize()` after taking charAt",
      "Compare with `charCodeAt(0)` instead"
    ],
    "answer": "Iterate code points: `[...str]` instead",
    "explanation": {
      "en": "length and charAt count UTF-16 units, so an emoji counts as 2; spreading walks code points."
    }
  },
  {
    "id": 119,
    "category": "python",
    "difficulty": "hard",
    "codeSnippet": "with open(path) as f:\n    for line in f:\n        rows.append(line)\nprint(rows[0] == rows[0].strip())",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Append `line.rstrip('\\n')` to the rows",
      "Append `line.rstrip('\\t')` to the rows",
      "Open the file in binary mode instead",
      "Read it all with `f.read()` in one go"
    ],
    "answer": "Append `line.rstrip('\\n')` to the rows",
    "explanation": {
      "en": "Iterating a file keeps the trailing newline on every line; strip it as you append."
    }
  },
  {
    "id": 120,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "function debounce(fn, ms) {\n  let t;\n  return (...a) => {\n    t = setTimeout(fn(...a), ms);\n  };\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Clear then defer: `clearTimeout(t)` and wrap",
      "Clear then defer: `clearInterval(t)` and wrap",
      "Return the timer id from the inner arrow",
      "Move `let t` inside the returned function"
    ],
    "answer": "Clear then defer: `clearTimeout(t)` and wrap",
    "explanation": {
      "en": "fn(...a) runs immediately and nothing cancels the previous timer, so it never debounces."
    }
  },
  {
    "id": 121,
    "category": "python",
    "difficulty": "hard",
    "codeSnippet": "def parse(rows):\n    return [int(r) for r in rows]\n\nparse(['1', 'x', '3'])",
    "question": {
      "en": "Which fix removes the crash?"
    },
    "options": [
      "Keep only digits: filter with `r.isdigit()`",
      "Keep only digits: filter with `r.isnumeric`",
      "Wrap each `int(r)` in a bare try block",
      "Change the list into a generator instead"
    ],
    "answer": "Keep only digits: filter with `r.isdigit()`",
    "explanation": {
      "en": "int() raises on a non-numeric string, which aborts the whole comprehension."
    }
  },
  {
    "id": 122,
    "category": "javascript",
    "difficulty": "hard",
    "codeSnippet": "const out = rows\n  .filter((r) => r.ok)\n  .map((r) => r.v)\n  .sort()\n  .slice(0, 3);",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Sort numerically with `(a, b) => a - b`",
      "Sort numerically with `(a, b) => a > b`",
      "Move `.slice(0, 3)` before the `.sort()`",
      "Replace `.filter` with `.flatMap` here"
    ],
    "answer": "Sort numerically with `(a, b) => a - b`",
    "explanation": {
      "en": "The default sort compares as strings, so the top three are wrong for numbers."
    }
  },
  {
    "id": 123,
    "category": "typescript",
    "difficulty": "hard",
    "codeSnippet": "type Cfg = { a: number } | { b: number };\nfunction read(c: Cfg) {\n  return c.a;\n}",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Narrow it: `if ('a' in c) return c.a;`",
      "Narrow it: `if (c.a) return c.a;` first",
      "Change the union into an intersection",
      "Mark both members of the union optional"
    ],
    "answer": "Narrow it: `if ('a' in c) return c.a;`",
    "explanation": {
      "en": "Only properties common to every union member are accessible until the type is narrowed."
    }
  },
  {
    "id": 124,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "const set = new Set([1, 2, 3]);\nconsole.log(set.length);",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Read the count with `set.size` instead",
      "Read the count with `set.count` instead",
      "Convert with `Array(set).length` first",
      "Call `set.values().length` to count it"
    ],
    "answer": "Read the count with `set.size` instead",
    "explanation": {
      "en": "A Set exposes size; length is undefined on it."
    }
  },
  {
    "id": 125,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "items = ['b', 'a']\nsorted(items)\nprint(items[0])",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Assign it: `items = sorted(items)` instead",
      "Assign it: `items = items.sorted()` instead",
      "Change `sorted(items)` to `items.sort` only",
      "Print `items[-1]` to read the first value"
    ],
    "answer": "Assign it: `items = sorted(items)` instead",
    "explanation": {
      "en": "sorted() returns a new list and leaves the original untouched; the result was discarded."
    }
  },
  {
    "id": 126,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "for (var i = 0; i < 3; i++) {\n  setTimeout(() => console.log(i), 0);\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Declare the counter with `let i` instead",
      "Declare the counter with `var i` instead",
      "Move the timeout call outside the loop",
      "Pass `i` as the third setTimeout argument"
    ],
    "answer": "Declare the counter with `let i` instead",
    "explanation": {
      "en": "var is function-scoped so every callback sees the final 3; let binds a fresh i per iteration."
    }
  },
  {
    "id": 127,
    "category": "python",
    "difficulty": "easy",
    "codeSnippet": "nums = [1, 2, 3]\nprint(nums.index(9))",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Check first: `if 9 in nums:` before index",
      "Check first: `if nums.has(9):` before index",
      "Change `index(9)` to `nums.find(9)` here",
      "Wrap the print call in a `str()` cast"
    ],
    "answer": "Check first: `if 9 in nums:` before index",
    "explanation": {
      "en": "list.index raises ValueError when the value is absent — unlike JS indexOf, it never returns -1."
    }
  },
  {
    "id": 128,
    "category": "javascript",
    "difficulty": "easy",
    "codeSnippet": "const n = '7';\nif (n === 7) {\n  run();\n}",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Convert first: `Number(n) === 7` in the test",
      "Convert first: `String(n) === 7` in the test",
      "Change `const n` to `let n` before the test",
      "Compare the value against `'7'` with `==`"
    ],
    "answer": "Convert first: `Number(n) === 7` in the test",
    "explanation": {
      "en": "Strict equality never matches across types; convert the string to a number."
    }
  },
  {
    "id": 129,
    "category": "python",
    "difficulty": "medium",
    "codeSnippet": "def scale(v, factor):\n    return v * factor\n\nprint(scale(factor=2))",
    "question": {
      "en": "What is the correct fix?"
    },
    "options": [
      "Pass both: `scale(3, factor=2)` in the call",
      "Pass both: `scale(factor=2, 3)` in the call",
      "Give `v` a default value in the signature",
      "Swap the two parameters in the signature"
    ],
    "answer": "Pass both: `scale(3, factor=2)` in the call",
    "explanation": {
      "en": "`v` has no default, so omitting it raises TypeError for a missing argument."
    }
  },
  {
    "id": 130,
    "category": "javascript",
    "difficulty": "medium",
    "codeSnippet": "const copy = rows;\ncopy.length = 0;\nconsole.log(rows.length);",
    "question": {
      "en": "Which fix removes the bug?"
    },
    "options": [
      "Copy it first with `const copy = [...rows]`",
      "Copy it first with `const copy = {...rows}`",
      "Change `copy.length = 0` to `copy = []`",
      "Freeze `rows` before making the copy"
    ],
    "answer": "Copy it first with `const copy = [...rows]`",
    "explanation": {
      "en": "Assignment shares the same array, so clearing the copy clears the original too."
    }
  }
]
